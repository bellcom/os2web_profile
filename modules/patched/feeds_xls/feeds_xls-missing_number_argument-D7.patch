diff --git a/modules/patched/feeds_xls/feeds_xls.module b/modules/patched/feeds_xls/feeds_xls.module
index a7bf99e..1526b93 100644
--- a/modules/patched/feeds_xls/feeds_xls.module
+++ b/modules/patched/feeds_xls/feeds_xls.module
@@ -238,6 +238,9 @@ function feeds_xls_feeds_set_target_list_text($source, $entity, $target, $value)
  * from a value to a key for list fields.
  */
 function feeds_xls_feeds_set_target_numeric($source, $entity, $target, $value){
+  $info = field_info_field($target);
+  $mapping['language'] = entity_metadata_field_get_language($entity->type, $entity, $info['field_name']);
+
   if(!is_array($value)){
     $value = array(
       $value
@@ -257,7 +260,7 @@ function feeds_xls_feeds_set_target_numeric($source, $entity, $target, $value){
       }
     }
   }
-  number_feeds_set_target($source, $entity, $target, $value, FALSE);
+  number_feeds_set_target($source, $entity, $target, $value, FALSE, $mapping);
 }
 
 /**
